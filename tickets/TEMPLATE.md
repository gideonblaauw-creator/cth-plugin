# Ticket Template — Blaauw Harness v3

Copy this block into a new file (e.g. `tickets/YYYY-MM-DD-short-name.md`) and fill in every field.
See `skills/harness/SKILL.md` for lane definitions and gate rules.

---

```yaml
desk:         # Which Desk owns this ticket (Socials | Inbox | Meetings | Grant | Proposal | SEO | Infra)
folder:       # Archive path or repo path where output lands
done-when:    # Single, testable completion criterion
lane:         # sonnet | haiku | flash | opencode
hitl:         # nothing sent/posted/paid without Gideon's approval
reviewer:     # Desk or person who signs off before HITL
context:      # Background, linked files, or prior ticket IDs the agent needs
```
