---
name: doctor-bot
description: >
  Invoke, interpret, or extend Doctor_Bot — CleantechHUB's infrastructure health
  agent that monitors MCP connectors, the OVH VPS (BookStack, Sustenttia Chroma,
  Caddy, MariaDB, TLS certs, disk, memory), and Pipedream workflows. Trigger on
  "run doctor_bot", "check infra health", "what's broken in my stack", "is
  BookStack up", "are my MCPs healthy", "check the VPS", or referencing the
  doctor-bot dashboard.
---

# doctor-bot

This file is a Cursor discovery stub. The playbook source of truth is `skills/doctor-bot/SKILL.md`.

1. Read `skills/doctor-bot/SKILL.md` and any `skills/doctor-bot/references/` files it points to before doing the work.
2. Follow that playbook. Do not invent a parallel workflow in this stub.
3. Do not copy VPS files from `/opt/claude-files` into this repo.

