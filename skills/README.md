# Plugin Skills — Editable Source

This is the **writable source** for all cleantechhub plugin skills.
Edit SKILL.md files here. Do NOT edit the cached copies in `~/Library/Application Support/Claude/`.

Each subdirectory is one skill. The skill name matches the directory name.
Changes here are picked up by Claude Desktop on next session/reload.
